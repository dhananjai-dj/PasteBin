
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pastebin
    username: dhananjai_dev
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  application:
    name: pasteBin
  security:
    user:
      name: dhananjai
      password: Ammu123*


server:
  port: 8080